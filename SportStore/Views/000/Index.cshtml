@* @model List<SportStore.Data.Entities.Product>

@{
    ViewData["Title"] = "Index";
    var categories = ViewBag.Categories as SelectList;
}

<h1>Products</h1>

<div>
    <form method="get" asp-action="Index" asp-controller="ManageProducts">
        <div class="form-group">
            <label for="CategoryId">Filter by Category:</label>
            <select name="categoryId" id="CategoryId" onchange="this.form.submit()">
                <option value="">All</option>
                @foreach (var category in categories)
                {
                    <option value="@category.Value" selected="@((category.Value == Context.Request.Query["categoryId"]).ToString().ToLower())">@category.Text</option>
                }
            </select>
        </div>
    </form>
</div>

<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Description</th>
            <th>Price</th>
            <!-- Add other columns as needed -->
        </tr>
    </thead>
    <tbody>
        @foreach (var product in Model)
        {
            <tr>
                <td>@product.Name</td>
                <td>@product.Description</td>
                <td>@product.Price</td>
                <!-- Add other columns as needed -->
            </tr>
        }
    </tbody>
</table> *@